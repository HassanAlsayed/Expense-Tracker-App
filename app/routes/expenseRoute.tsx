import { SafeAreaView, Text } from "react-native";
import style from "./style";

export const ExpenseRoute = () => (
  <SafeAreaView style={style.scene}>
    <Text style={style.text}>Expense Screen</Text>
  </SafeAreaView>
);